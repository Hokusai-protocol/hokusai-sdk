---
'@hokusai/core': minor
---

Add a shared `HokusaiClient` for route and outcome API calls with typed payloads, retries, request IDs, SDK version headers, dry-run validation, and actionable error classes. Rename the previous offline dispatch helper to `HokusaiDispatchBuilder`.
