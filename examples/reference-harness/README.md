# Reference Harness

This example is the smallest offline template for composing `@hokusai/core` with one harness adapter.

It is intentionally limited to static metadata and deterministic task preparation. No network transport or harness runtime is required.
