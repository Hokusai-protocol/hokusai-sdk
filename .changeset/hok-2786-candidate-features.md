---
'@hokusai/core': minor
---

Publish the strict, versioned `candidate_features/v1` Arbiter contract with nullable derived fields, runtime and JSON Schema validation, task-descriptor projection, group manifests, and cross-repo fixtures.
