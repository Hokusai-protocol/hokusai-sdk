---
'@hokusai/core': minor
---

Add a reusable host-integration kit to `@hokusai/core` with `HostAdapter`,
`runHokusaiLoop`, `HokusaiLoopClient`, and `asHokusaiLoopClient` so new
route/contribute harnesses can share the reference-harness loop without copying
it.
