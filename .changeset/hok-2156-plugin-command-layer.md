---
'@hokusai/core': minor
'@hokusai/adapter-claude-code': patch
---

Extract the shared plugin command layer into `@hokusai/core` and bind the Claude adapter through a harness profile with enforced Anthropic routing constraints.
