---
'@hokusai/core': patch
---

Add an OpenHands routing example that maps Hokusai recommendations onto
OpenHands `RouterLLM` model choices, extracts SDK metrics, and submits
`harness_outcome_row/v1` contributions through the shared request shape.
